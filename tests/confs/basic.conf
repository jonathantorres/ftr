# This is a comment, this should be ignored
server = localhost
port = 20
root = /Users/jonathantorres/ftrd_test
error_log = /etc/log/ftrd/errors.log
access_log = /etc/log/ftrd/access.log

user {
    username = jt
    password = test
    root = /jt
}

user {
    username = test
    password = test
    root = /test
}
