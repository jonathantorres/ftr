# This is a comment, this should be ignored
server = localhost
port = 20
root = /Users/jonathantorres/ftpd_test
error_log = /etc/log/ftpd/errors.log
access_log = /etc/log/ftpd/access.log

user {
    username = jt
    password = test
    root = /jt
}

user {
    username = test
    password = test
    root = /test
}
