# This is a comment
server = localhost # you can use a hostname or an IP address
port = 20 #you can use a reserved port or any other port
root = /home/jt/ftr_test
error_log = /etc/log/ftr/errors.log
access_log = /etc/log/ftr/access.log

# Include a file
#include some_file.conf

user {
    username = test1
    password = test1 #use secure passwords
    root = /test1
}
user {
    username = test2
    password = test2
    root = /test2
}
user {
    username = test3
    password = test3
    root = /test3
}

